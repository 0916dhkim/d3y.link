version: "3.8"

services:
  frontend:
    build:
      context: ./FrontEnd
    networks:
      - d3y

  backend:
    build:
      context: ./BackEnd
    environment:
      - PORT
      - DATABASE_URL
    networks:
      - d3y

networks:
  d3y:
    external: false
